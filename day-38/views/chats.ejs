<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet"  href="style.css">
</head>

<body>
    <h1>Chats Page</h1>
    <p>Welcome to the chats page!</p>
    <form action="/chats/new">
        <button> Write New Chat </button></form>    
    <% for(let chat of chats) { %>
        <div class="chat">
            <p>from:<%= chat.from %>
            </p>
            <div class="msg">
                <p>
                    <%= chat.msg %>
                </p>
                <form method="GET" action="/chats/<%= chat.id %>/edit">
                    <button>edit</button>
                </form>
            </div>
            <p>Received by : <%= chat.to %></p>
            <hr>
            <p>Date: <%= chat.created_at.toString().split(" ")[4] %> </p>
            <p><%= chat.created_at.toString().split(" ").splice(0,4).join(" ")%></p>

        </div><br><br>
        
    <% } %>
    
</body>

</html>